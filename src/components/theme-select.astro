---
/* Renders nothing, on purpose. The sites are light-only — ADR-0001 — so there
   is no theme to select. Suppressing the picker is half the job; ThemeProvider
   in this directory does the other half. */
---
